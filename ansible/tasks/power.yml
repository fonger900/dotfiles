---
- name: Power | Ensure TLP service is enabled
  ansible.builtin.systemd:
    name: tlp
    enabled: yes
    state: started
  become: true

- name: Power | Configure TLP for ThinkPad T14 Gen 1 AMD
  ansible.builtin.copy:
    dest: /etc/tlp.d/99-thinkpad-t14.conf
    mode: '0644'
    content: |
      # TLP config — ThinkPad T14 Gen 1 AMD (Ryzen 4650U / Renoir)

      # ── Battery health ───────────────────────────────────────────
      # Conservative thresholds for daily plugged-in use
      START_CHARGE_THRESH_BAT0=75
      STOP_CHARGE_THRESH_BAT0=80

      # ── CPU ──────────────────────────────────────────────────────
      CPU_SCALING_GOVERNOR_ON_AC=schedutil
      CPU_SCALING_GOVERNOR_ON_BAT=schedutil

      CPU_ENERGY_PERF_POLICY_ON_AC=balance_performance
      CPU_ENERGY_PERF_POLICY_ON_BAT=power

      CPU_BOOST_ON_AC=1
      CPU_BOOST_ON_BAT=0

      CPU_HWP_DYN_BOOST_ON_AC=1
      CPU_HWP_DYN_BOOST_ON_BAT=0

      # ── Platform profile (AMD) ───────────────────────────────────
      PLATFORM_PROFILE_ON_AC=balanced
      PLATFORM_PROFILE_ON_BAT=low-power

      # ── GPU (Renoir iGPU) ────────────────────────────────────────
      RADEON_DPM_PERF_LEVEL_ON_AC=auto
      RADEON_DPM_PERF_LEVEL_ON_BAT=low

      # ── PCIe / ASPM ─────────────────────────────────────────────
      PCIE_ASPM_ON_AC=default
      PCIE_ASPM_ON_BAT=powersupersave

      # ── WiFi ─────────────────────────────────────────────────────
      WIFI_PWR_ON_AC=off
      WIFI_PWR_ON_BAT=on

      # ── Audio ────────────────────────────────────────────────────
      SOUND_POWER_SAVE_ON_AC=0
      SOUND_POWER_SAVE_ON_BAT=1
      SOUND_POWER_SAVE_CONTROLLER=Y

      # ── USB autosuspend ──────────────────────────────────────────
      USB_AUTOSUSPEND=1
      # Exclude USB input devices from autosuspend (keyboard/mouse)
      USB_EXCLUDE_BTUSB=1

      # ── Runtime PM ───────────────────────────────────────────────
      RUNTIME_PM_ON_AC=on
      RUNTIME_PM_ON_BAT=auto
  become: true
  notify: Restart TLP

- name: Power | Disable thermald (conflicts with AMD P-state driver on Ryzen)
  ansible.builtin.systemd:
    name: thermald
    enabled: false
    state: stopped
  become: true
  ignore_errors: true

- name: Power | Enable AMD P-state driver via kernel cmdline
  ansible.builtin.lineinfile:
    path: /etc/default/grub
    regexp: '^GRUB_CMDLINE_LINUX_DEFAULT='
    line: 'GRUB_CMDLINE_LINUX_DEFAULT="quiet splash amd_pstate=active nvme.noacpi=1"'
    backup: yes
  become: true
  register: grub_changed
  notify: Update GRUB

- name: Power | Set AMD GPU power profile on AC (udev rule)
  ansible.builtin.copy:
    dest: /etc/udev/rules.d/91-amdgpu-power.rules
    mode: '0644'
    content: |
      # Set amdgpu power profile based on AC/battery
      ACTION=="change", SUBSYSTEM=="power_supply", ATTR{online}=="1", \
        RUN+="/bin/sh -c 'echo auto > /sys/class/drm/card0/device/power_dpm_force_performance_level'"
      ACTION=="change", SUBSYSTEM=="power_supply", ATTR{online}=="0", \
        RUN+="/bin/sh -c 'echo low > /sys/class/drm/card0/device/power_dpm_force_performance_level'"
  become: true
